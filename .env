# dev (works with your existing Vite proxy to localhost:8000)
printf "VITE_API_BASE_URL=/api\n" > .env.development

# production (until DNS is ready, use your droplet IP)
printf "VITE_API_BASE_URL=http://i4wash.com/api\n" > .env.production